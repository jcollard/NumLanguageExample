package lang;

import lang.parser.Parser;

public class ParserFactory {

	/**
	 * Given the next token in a file, return the next parser to use to parse.
	 * @param token The token to be parsed
	 * @return a Parser that knows how to parse the next token
	 */
	public Parser getParser(String token) {
		return null;
	}
	
}